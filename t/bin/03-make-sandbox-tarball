#!/usr/bin/env bash

pushd "$(dirname $0)/../..";

init=(
    'rm -rf'
    'mkdir'
    'git init'
    'cd'
);

for i in "${init[@]}"
do
    $i ./sandbox;
done

for i in ../version/*; 
do
    (
        cd $i;
        find . | cpio -pdv ~-;
    );

    echo -e "$i\n$(ls -al)";
   
    git add *;
    git commit -m"$i";
    git tag -a "perl/$(basename $i)" -m"$i";
    git commit -m"tag $i";

    rm -rf *;
done

# remove any cruft, push the empty head.
rm -rf *;
git add .;
git commit -m'empty directory';

tar cf .git.tar .git;

rm -rf .git;

exit 0;

